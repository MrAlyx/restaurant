{% extends 'base.html.twig' %}

{% block title %}Reservering{% endblock %}

{% block body %}
    <h1>Reservering</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservering.id }}</td>
            </tr>
            <tr>
                <th>AantalPersonen</th>
                <td>{{ reservering.aantalPersonen }}</td>
            </tr>
            <tr>
                <th>Datum_tijd</th>
                <td>{{ reservering.datumTijd ? reservering.datumTijd|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservering_index') }}">back to list</a>

    <a href="{{ path('reservering_edit', {'id': reservering.id}) }}">edit</a>

    {{ include('reservering/_delete_form.html.twig') }}
{% endblock %}
