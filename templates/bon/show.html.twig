{% extends 'base.html.twig' %}

{% block title %}Bon{% endblock %}

{% block body %}
    <h1>Bon {{ bon.id }}</h1>
    <h2>datum : {{ bon.datumTijd ? bon.datumTijd|date('Y-m-d') : '' }}</h2>

    <table class="table table-striped pull-right">
        <thead>
            <tr>
            <td>Aantal</td>
            <td>Product</td>
            <td>Prijs</td>
            <td>Totaal</td>
            </tr>
        </thead>
        <tbody>
        <tr>
{#            <td>{{ reservering.bestelling.aantal }}</td>#}
        </tr>
        </tbody>
    </table>

    <a href="{{ path('bon_index') }}">back to list</a>

    <a href="{{ path('bon_edit', {'id': bon.id}) }}">edit</a>

    {{ include('bon/_delete_form.html.twig') }}
{% endblock %}
